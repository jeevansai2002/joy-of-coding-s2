
#include<stdio.h>
#include<stdlib.h>
int main(){
	int n,i,k,j;
	int found=0;
	printf("enter the no.of integers: ");
	scanf("%d",&n);
	int a[n];
	printf("enter the array :");
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
	}
	printf("enter the sum value : ");
	scanf("%d",&k);
	for(i=0;i<n;i++){
		for(j=i+1;j<n;j++){
			if(k==(a[i]+a[j])){
				found=1;
				printf("found the sum in the indeses %d and %d\n",i,j);
				exit(0);
			}
		
		}
	}
	if(found==0){
		printf("no indices found\n");
	}
	return 0;
}
