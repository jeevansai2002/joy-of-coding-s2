#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(){
	int a[50],b[50];
	int len,mid;
	printf("enter the string 1 :\n");
	scanf("%[^\n]s",a);
	printf("enter the second string :\n");
	scanf("%[^\n]s",b);
	for( len=0;a[len]!='\0';len++);
	mid=len/2;
	for(int i=0;i<mid;i++){
		if(a[i]==)
	}


}
